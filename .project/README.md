# Elements project metadata

- **`APG-JS-ROADMAP.md`** — W3C APG pattern parity: what `UI8Kit/js` already covers vs backlog for **`Elements/js`** (load `ui8kit` first, add missing patterns).
- **`VALIDATION-AND-TOOLING.md`** — linting and a11y checks: what to run in Elements vs Blocks vs apps; why not to copy the full `w3c/aria-practices` package stack verbatim.
